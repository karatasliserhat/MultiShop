@model GetProductDetailWithProductDto

<div class="tab-pane fade show active" id="tab-pane-1">
    <h4 class="mb-3">Ürün Açıklaması</h4>
    @if (Model is not null)
    {
        <p>@Model.Descrpition</p>
    }
    else
    {
        <p>Ürün Açıklaması Yok</p>
    }
</div>